{
  "name": "typeit",
  "private": true,
  "version": "0.1.5",
  "description": "Visual shell for the TypeIT widget — a frameless, translucent, always-on-top desktop window (appearance/architecture study only).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
