{
  "name": "ghost-typer-shell",
  "private": true,
  "version": "0.1.0",
  "description": "Visual shell for the Ghost Typer widget — a frameless, translucent, always-on-top desktop window (appearance/architecture study only).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
